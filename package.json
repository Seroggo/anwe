{
  "name": "anwe-project",
  "version": "0.1.0",
  "private": true,
  "description": "ANWE SiteBlueprint library and Block Library Alpha",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "icons": "astro dev --open /dev/icons/",
    "build": "astro build",
    "check:json": "node scripts/check-json.mjs",
    "check:blueprints": "node scripts/check-blueprints.mjs",
    "check:blocks": "node scripts/check-block-fixtures.mjs",
    "check:schemas": "node scripts/check-schemas.mjs",
    "check:site-model": "node scripts/check-site-model.mjs",
    "check:machine": "node scripts/check-machine-spec.mjs && node scripts/check-machine-spec-negative.mjs",
    "check:astro": "astro check",
    "check:theme": "node scripts/check-theme-compiler.mjs",
    "check:human-build": "node scripts/check-human-build.mjs",
    "check:theme-build": "node scripts/check-theme-build.mjs",
    "check": "npm run check:json && npm run check:blueprints && npm run check:blocks && npm run check:schemas && npm run check:site-model && npm run check:machine && npm run check:theme && npm run check:astro"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "ajv": "^8.20.0",
    "astro": "^7.3.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@lucide/astro": "^1.45.0"
  }
}
